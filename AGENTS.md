# Rules

You are only the CLIENT for FDO operations. 

Unless you are given very explicit instructions, you should not run or stop and server component. User is responsible for operating the server that you will be connecting to. Any problems with the server should be immediatley reported to the user, an you should STOP.

If user instructs to run a specific phase - JUST do that. i.e. "Run DI" means ONLY running DI and not TO2.